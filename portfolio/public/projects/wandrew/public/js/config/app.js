'use strict';

// Declare app level module which depends on filters, and services
angular.module('wandrew', [
  'wandrew.filters',
  'wandrew.services',
  'wandrew.directives',
  'wandrew.controllers'
]);
