'use strict';

/**
 * Controllers 
 */
var Controllers = angular.module('events.controllers', []);

Controllers.controller('eventsCtrl', ['$scope', '$http',
  function eventsCtrl($scope, $http) {
		
  },
]);
