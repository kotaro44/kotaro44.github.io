'use strict';

// Declare app level module which depends on filters, and services
angular.module('events', [
  'events.filters',
  'events.services',
  'events.directives',
  'events.controllers',
]);
